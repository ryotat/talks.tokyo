<div>
    <%= add_talk_to_list_button %>
    <%= vcal_button %>
    <% if User.current %>
    <%= tell_a_friend_button %>
    <% end %>
    <%= contact_organizer_button %>
  <% if @talk.editable? %>
  <div class="btn-group">
    <%= link_to icon_tag('icon-cog'), '#', :class => "btn dropdown-toggle", :data => { :toggle => "dropdown"} %>
    <ul class="dropdown-menu">
      <li><%= link_to icon_tag('icon-pencil')+'Edit this talk', {:controller => "talk", :action => "edit", :id => @talk} %></li>
      <li><%= link_to icon_tag('icon-wrench')+'Delete this talk', {:controller => 'talk', :action => 'delete', :id => @talk} %></li>
    </ul>
  </div>
  <% end %>
</div>
