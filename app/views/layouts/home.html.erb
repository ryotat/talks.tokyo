<!DOCTYPE html>
<html lang="en">
<%= render 'shared/header' %>
<body>
  <%= render 'shared/navigationbar' %>
  <div class="container">
    <%= render 'shared/breadcrumbs' %>
    <%= show_flash %>
    <div class="row">
      <%= yield %>
    </div>
  </div>
  <%= render 'shared/footer'%>
</body>
</html>
