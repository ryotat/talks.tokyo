<% @errors.each do |error| %>
Error: <%=error%>. 
<% end %>

<%= @talks.empty? ? '(No talks)' : ''%><% @talks.group_by(&:date).each do |date,talks| %>
<%= l date, :format => :long  %>
---------------------------
<% talks.sort_by { |talk| talk.start_time }.each do |talk| %>
<%= talk.start_time.strftime('%H:%M') %> <%= talk.title %><% end %>
<% end %>

<%= t('show.email.detail') %>
*********
<% @talks.group_by(&:date).each do |date,talks| %>
<%= l date, :format => :long %>
---------------------------
<% talks.sort_by { |talk| talk.start_time }.each do |talk| %>
<%= talk.series.name %>
<%= talk.title.upcase %>
 <%= talk_url(talk) %>
 <%= talk.name_of_speaker %><% if talk.special_message && !talk.special_message.empty? %><%= "\n #{talk.special_message}" %><% end %>
 <%= talk.start_time.strftime('%H:%M') %>-<%= talk.end_time.strftime('%H:%M') %> <%= talk.venue && talk.venue.name %>
<%= talk.abstract %>

<% end %><% end %>
